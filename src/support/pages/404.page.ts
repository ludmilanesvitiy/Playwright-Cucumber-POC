import { BasePage } from './base.page';

export class NotFoundPage extends BasePage {
    h2 = ('h2');
    h3 = ('h3');
    pageImg = ('not-found-page img');
    pageBtns = ('.not-found-page_content button');
}